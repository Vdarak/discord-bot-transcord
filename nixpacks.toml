[phases.setup]
nixPkgs = ['nodejs_20', 'python3', 'pkg-config', 'gcc', 'make', 'libsodium-dev', 'libopus-dev', 'ffmpeg']

[phases.install]
cmds = ['npm ci --only=production --no-audit --no-fund']

[phases.build]
cmds = ['echo "No build step required"']

[start]
cmd = 'npm start'